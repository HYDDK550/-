---
title: 6.11学习内容
created: '2025-06-09T18:10:42.626Z'
modified: '2025-06-13T13:13:49.163Z'
---

# 6.11学习内容
## 论文阅读：In-Datacenter Performance Analysis of a Tensor Processing Unit
一篇比较早期的文献，论述的是如何评估TPU加速器的性能分析。阅读该文章的主要目的是搞清楚上周文章中对加速器一些指标的计算和分析的基本概念。

### 名词解释
![表一](https://i-blog.csdnimg.cn/direct/b9578ffda47c4299b7d24aaba5e9f111.png)

表一展示了在谷歌数据中心部署的的TPU加速器运行不同神经网络时的参数，神经网络层数和LOC似乎是无关紧要的，重要的是网络权重weight与TPU计算能力OP/s的关系。

1.**TOPS**：trillion OP/s，衡量计算能力的指标。例如

TPU的核心计算单元是一个 256 x 256 的 8-bit MAC 阵列，总共：256 × 256 = 65,536 个 8-bit 乘加单元（INT8）
每个乘加（MAC）= 2 个操作（乘法 + 加法）
时钟频率为 700 MHz

所以理论峰值算力计算为：

65536MACs * 2op/s * 700Mhz * 约等于92TOPS
 
​2.**Weights**	神经网络中训练得到的权重参数总数（8-bit），用于推理阶段。

**TPU Ops / Weight Byte**，衡量**Weight Reuse** 权重复用率的指标，之前在很多论文中看到的关键词，定义是：衡量每1字节权重能触发多少计算

3. **batch Size** ：并行数据量，模型的batch Size越高对带宽要求就越高。

### 对加速器设计的指导

![Roofline](https://i-blog.csdnimg.cn/direct/5e11ca21c3684a0aa8c77169b6857b32.png)
上图是基于Roofline模型的硬件性能图，
X轴（横轴）表示操作强度（Operational Intensity），每读取 1 字节内存数据执行的操作次数（单位：ops/byte）

Y轴（纵轴）表示程序性能（Performance），单位一般为TOPS（每秒多少次计算）

一看图就知道是带宽问题还是算力问题，如果一个点落在 斜线以下说明内存带宽限制，表示程序 Ops/Weight Byte 太低，计算单元闲置，主存读得不够快。如果某模型 ops/byte 太低，可以通过 batch 提高、算法重构等优化。如果点在 水平线附近则表示计算能力限制，表示程序已经几乎“榨干”了计算能力，进一步优化需增加硬件算力。具体的权重复用优化方法可以查看6.6学习内容。


## 论文阅读：基于FPGA的可重构高能效神经网络加速器的研究
这篇文章和我读的一本书《基于fpga的深度学习加速器的设计与实现》的技术路线很相似，做的是可重构的FPGA加速平台，也是针对CNN（其实不止CNN，MAC操作估计都可以加速）
该文的工作可以分为两个层面：一是优化计算算子，研究空间并行概率乘法器的精度与功耗表现；二是优化加速器架构与数据流设计，以提高整体计算效率与资源利用率。
其他的类似整个系统的架构也是用DMA访问寄存器做加速器的配置，我也找了一些类似开源项目：

1.https://github.com/Jiajun-Ji/CNN_Accelerator?utm_source=chatgpt.com（HLS到Verilog）
2.https://github.com/WalkerLau/Accelerating-CNN-with-FPGA/blob/master/README_CN.md?utm_source=chatgpt.com（一个16位的项目啊，）
3.https://github.com/Futuresxy/General-CNN-Accelerator?utm_source=chatgpt.com（牛的，几乎是纯VHDL）

### 算子层面的优化
算子是围绕“空间并行概率乘法器（SPSC Multiplier）”做的深入研究，提出了一种高精度、低功耗的改进方案，旨在提升神经网络推理计算中的能效比。
传统的概率计算通过概率编码器将数值转化为概率序列，再用逻辑门进行运算，虽然电路简单，但在精度提升时需大幅增加编码长度，导致硬件开销和时延急剧上升。
也是MAC算子的通病了，位宽一上去占用的资源太多。优化方法有三个：

1.对空间并行概率乘法器的计算误差进行系统建模，指出误差主要来源于部分项的权重偏差。在此基础上，提出了基于权重修正的误差补偿机制，有效减少偏差与误差方差，实现计算精度显著提
2.构建了有符号高精度SPSC乘法器，通过将有符号乘法分解为多个无符号乘法模块进行组合，拓展了乘法器的适用范围。
3.针对输入位宽扩大所带来的指数级硬件开销问题，作者提出了一种同项合并优化策略：利用加法树中的高权重加法器提升高位部分项的合并效率，从而压缩空间编码序列长度，减少逻辑资源使用和功耗。

### 架构的优化
架构方面之前也读过很多相关的文章，能看的明白一些方法了。把FPGA的资源限制看作数学模型，然后研究约束下的最小延时问题。

![在这里插入图片描述](https://i-blog.csdnimg.cn/direct/f4df7bcef20b4872a29777a7270276e0.png)
分析了无存储限制条件下的数据传输模型，分别建模激活值和权值的加载方式，结合阵列流水线调度机制优化计算时序，提升阵列利用率。
而在更具现实性的片上存储受限场景下，他进一步提出了输入分块策略，探讨权值分块、激活值分块与双分块三种方式，并对其进行定量实验比较。
#### 思考：这里我看他用的是ZYNQ，目前来看我觉得使用Xilinx 的 UltraScale系列芯片会不会好点，之前看过一篇文章是使用FPGA做调度然后用UltraScale上的DSP阵列做计算的。

## 工程知识的学习

1.Chisel，做了调研，动手配置了个环境，找到一本教材《chisel-book》。我的感觉是Chisel目前只是一个HDL的生成器，而且生成的代码听说对于后端的验证和时序收敛是不太友好的。和HSL相比的话，无论是Intel还是Xilinx都有完整的HSL开法链，Github上的开源项目FPGA加速器项目要么使用的纯Verilog和HDL，要么是HSL。目前chipyard环境中用scala的比较多，都是伯克利的研究框架。
