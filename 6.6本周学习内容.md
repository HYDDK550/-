---
title: 6.6本周学习内容
created: '2025-05-16T07:59:32.386Z'
modified: '2025-06-06T08:19:01.839Z'
---

# 6.6本周学习内容
### AGENT的尝试
1.尝试学习一些AI的工作流，如Cursor和KOZE这些，希望可以通过AGENT来降低一些学习代码的时间成本。Cursor目前能接入的大模型API有deepseek的V3和Gemini。不知道能不能找到免费的CLAUDE 4.0 sonnet或者GPT 4.1这些API。

### HLS的学习
2.初步学习阶段，了解HLS的设计流程、工作机制，让我感觉是软件工程师转硬件的一个很好的工具，但与Verilog的原生代码相比会造成时序的浪费。但没办法，直接用Verilog描述加速器太过于痛苦，探索一下AGENT能不能解决这个问题。

### 论文阅读
3.《通过比特权重方法处理MAC来提升TPE的性能》这篇文章虽然研究的是TPU的加速器中的PE优化方法，但最后也是一个优化RTL的过程，这些优化方法应该是通用的。但优化方法中涉及到的权重位乘法仍然需要进一步的理解，需要阅读《In-Datacenter Performance Analysis of a Tensor Processing Unit》和《Laconic deep learning inference acceleration》这两篇文章才能深入理解架构和能效、性能、面积之间的关系。

# 阅读文献：通过比特权重方法处理MAC来提升TPE的性能
原文标题：Exploring the Performance Improvement of Tensor Processing Engines through Transformation in the Bit-weight Dimension of MACs

一句话总结：论文中主要聚焦于TPU芯片中的TPE优化问题，基于权重位乘法提出了四种可行的优化方法并且应用在四种TPE架构中，在 SMIC 28nm 工艺下进行 RTL 实现与综合测试，取得了性能提升。
## 关键词
TPU：专用的AI加速ASIC，由PE组成
TPEs：张量处理引擎，用来处理MM（矩阵乘法）
multiplyaccumulator ，MAC：TPEs中的主要单元--乘积累加运算单元。
Bit-slice-based multiplication 权重位乘法：文中一种将多位二进制数的乘法计算在比特层级上切分重构的策略，强调按权重位（bit weight）进行操作，以提高计算的稀疏性利用、资源共享和压缩效率。

## 疑问？
### 1. TPU芯片与GPU和FPGA的架构完全不同，其中实现矩阵乘法的基本单元是TPEs，那么TPEs能在FPGA上实现吗，效能和灵活性相比TPU会如何？
（1）基于 Vitis AI 框架部署

 Xilinx 提供的 Vitis AI 框架可部署 AI 模型至 DPU（深度学习处理单元）——一种典型的可编程 TPE；
 可支持 PyTorch、TensorFlow 模型转化；

（2）用 HLS（高层次综合）

使用 C/C++ 描述 MAC/TPE 结构，Vivado HLS 或 Vitis HLS 综合为 RTL；

（3）使用 RTL + IP 定制（这个太麻烦了）

使用 Verilog/VHDL 编写优化后的 MAC、PE阵列、控制器；

### 2. 文章中具体是怎么优化TPEs的？该方法对其他架构的芯片有无帮助？

#### A 总的来说TPE的工作分为三个步骤：
* **PPG（Partial Product Generation）部分积生成**

该阶段接收两个输入操作数（如一个激活值和一个权重），并生成它们的位乘结果（bitwise product）。
实现方法通常包括：AND门阵列、Booth 编码、移位加法等。

* **Compressor Tree（或 Partial Product Compression）——部分积压缩**

将前一阶段生成的大量部分积通过加法器（如 Wallace Tree、Dadda Tree 或普通的carry-save adder阵列）进行**层层压缩**，最终减少为两个或三个中间结果。
**降低部分积数量，提升加法效率**，通常不立即输出最终结果，而是为累加阶段准备。

* **Final Accumulation / Adder Tree（最终累加）**

将压缩后的中间结果进行最终加法，输出完整的乘法结果；
 MAC 运算（如矩阵乘法），此阶段还包括对前一个累加结果的读取与加法操作；
这一阶段常采用**可变位宽的加法器、带进位处理的定点加法器等**，资源占用较大，常是优化瓶颈。


#### B 那么 bit-slice-based 权重位乘法是怎么优化TPE的呢？

传统乘法计算通常按如下方式处理：对两个 N 位二进制数相乘，会生成 N×N 个“部分积”（partial product），然后逐位累加。

而 bit-slice-based 方法的核心不同点是按权重位分组（bit weight slicing）：
一：不直接生成所有部分积，而是按每一位权重（$2^0, 2^1, ..., 2^n$）将多个输入操作数组织在一起，所有来自不同乘法操作、但属于同一权重位的位，被一起压缩或处理。
二：从空间展开转为时间展开（spatial → temporal）：不为每个位权展开物理电路，而是复用计算资源，用多个周期串行处理不同权重位。
三：跳过0位（sparse-friendly）：如果某一位是0，相关部分积直接被忽略，实现稀疏计算。可与编码器（如Booth编码）配合，跳过冗余操作。

具体做法是论文将传统 MAC 操作转化为形式化表达，并将其划分为：PPG（Partial Product Generator）,Compressor（压缩器）,Shifter（位移器）,Encoder（编码器）
在 bit-slice 架构中：
PPG 被共享：每个位权共用一个PPG资源；
Compressor 使用统一结构，对同一位权位进行压缩；
Shifter 替代加法器进行移位，避免大位宽加法器；
Encoder控制逻辑将位权按时间顺序处理，降低面积、功耗。

#### C bit-slice-based MAC 的主要问题：
1. 存在冗余部分积生成（Partial Product Generation 冗余）
每一位权重对应的乘法操作都被单独处理，即使该位为零，也仍然生成对应的部分积。

导致在输入数据稀疏或低比特情况下，仍有大量无效计算，浪费功耗和面积。

论文指出：即使是 INT8 数据，也要为每一个非零 bit weight 都设计单独的 PPG 通道，导致面积利用率低。

2. 加法器开销大（Final Accumulation Area Overhead）
bit-slice 方法需要为每个位权设置完整的累加路径（shifter + adder），即：

每一位产生的部分积都要右移再送入一个独立加法器。

当精度较高（如 INT16）时，需要多个并行累加器，造成面积爆炸。

3. 控制逻辑复杂，流水线调度难
不同位权的操作在时间上交错进行，需要复杂的调度器来管理 shifter、compressor、adder 等模块的激活顺序。

增加了控制路径延迟和设计复杂度。

4. 编码器与路径冗余，资源利用低
每个 PPG 通常都需要独立的编码器（如Booth、Radix-N等），但这些编码器在实际过程中是可共享的。

原始 bit-slice MAC 中并未共享编码逻辑，导致逻辑资源重复使用，能效下降。

所以具体的优化方式就有OPT1到OPT4就是针对以上环节的问题进行优化
|优化策略	|对应解决问题|
|:---:|:----------:|
|OPT1	|用 compressor 替代 adder，减少 final accumulation 面积|
|OPT2	|将 bit weight 空间展开改为时间展开，减少并行累加器|
|OPT3	|引入 sparse 编码，跳过为 0 的位，避免冗余 PPG 操作|
|OPT4	|将多个编码器提取为共享逻辑，大幅度节省重复资源|




