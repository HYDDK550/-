---
title: FPGA深度学习加速器的技术栈
created: '2025-05-15T06:12:13.196Z'
modified: '2025-05-17T09:12:55.237Z'
---

# FPGA深度学习加速器的技术栈
## 1.引言—从算法到硬件
&emsp;&emsp;深度学习（Deep Learning）是机器学习的一个重要分支，以人工神经网络（artificial neural network) 为基础架构，能对样本数据进行学习的算法都可以被归类于深度学习算法。如今深度学习被广泛应用于人脸识别、自动驾驶、云服务等领域。
&emsp;&emsp;随着神经网络越来越复杂，基于冯诺依曼计算架构的CPU硬件平台难以满足神经网络的计算需求。这是因为该体系遵循取指、译指、执行的运作流程，需要大量指令调度操作，占用了大量的片上资源，不能完成对神经网络进行加速的硬件指标。
&emsp;&emsp;GPU、ASIC和FPGA是当下比较流行的神经网络硬件加速平台。GPU压缩了控制逻辑和片上缓存，并且带有大量SIMD（单指令多数据流单元）使其具有很强的浮点计算能力和并行计算能力。但GPU使用了大量的流处理计算单元，使其功耗很高，一般用于训练初期或云计算领域。专用的深度学习芯片ASIC提供了不逊色于GPU的峰值计算能力和低得多的功耗（GPU的百分之一）但开发一款专用的定制芯片无疑意味着高成本、长开发周期的问题，失败的风险极大。FPGA作为一种折中的解决方案，凭借高并行处理能力、低功耗和可重复配置的特点展现出在深度学习加速上的潜力，但开发难度和较高的价格让其无法广泛的应用。

&emsp;&emsp;下表展示了上述四种硬件平台在深度学习领域的优缺点。
|芯片|优点|缺点|  
 |:---:|:----------:|:-----------:|
|CPU|通用性强，适合逻辑控制|功耗高，并行处理能力差|
|GPU|浮点计算能力很强，支持大量数据的并行运算|功耗高，控制管理能力弱|
|ASIC|定制化设计，功耗低，效率高|开发成本和难度高|
|FPGA|灵活性强，功耗低|开发难度大|

&emsp;&emsp;本文的重点是基于FPGA平台的硬件加速器，对开发流程以及每个流程中需要用到的技术和软件作了整理，也给出了一些常用的深度学习编译器网站供读者进行检索。
## 2.FPGA加速器的技术路线
&emsp;&emsp;基于FPGA平台的深度学习加速器无疑是一项复杂的工作，这里指的是可以实现一个工程上可以使用的深度学习加速器平台而不是Github的一些HLS工程代码。根据加速器的灵活性和性能差异，可以将深度学习加速器分为三类：

1. 专用深度学习加速器
这种加速器将某个特定神经网络硬编码到 FPGA 内部进行加速。优点在于 FPGA 的设计是针对某一网络模型优化的，具有高性能、良好的稳定性。但其缺点是模型结构固定，灵活性差，需要重新设计。由于没有配置需求，因此不需要编译器和配置工具。

2. 可配置深度学习加速器
将 FPGA 内的加速计算单元做成可配置模块，可以配置常见神经网络层级，并可配置层级组合和调度策略，从而实现灵活的神经网络结构加速。这类加速器性能比第一类低，但具有参数配置灵活的优点。通常配置信息结构简单，仅需简单工具生成配置信息，而不涉及复杂的编译流程。

3. 深度学习（算子）加速器
这类加速器可以支持 FPGA 内多种网络算子加速，网络层的组合与调度由上层软件完成。该类加速器的灵活性最好，但由于指令和配置可调度，存在一定性能损耗，并需要编译器支持来解析配置指令。因此类似通用处理器，需要专用编译器。

&emsp;&emsp;然而无论是哪一类，从算法到具体部署到FPGA上的RTL代码并不是一个简单的过程，这其中涉及到的技术或开发平台大致可以分为软件开发（前端）、软件转硬件开发（端对端）、硬件实现三个部分。当然除了这三个核心部分还有像上位机开发、API接口设计和设计过程中的验证技术等内容，这取决于要实现的深度学习加速器是上述的哪一种。

![请添加图片描述](https://i-blog.csdnimg.cn/direct/bd8309837eec4acda6ea47847e895be5.png)


## 3.FPGA加速器的开发流程


   ![在这里插入图片描述](https://i-blog.csdnimg.cn/direct/a011921eacf74b9084eecface4a09369.png#pic_center)
   

### 3.1算法的构建
&emsp;&emsp;神经网络算法的构建工作一般由精通Pytorch或TensorFlow的软件工程师承担，该成员要能够及进行神经网络的设计、优化和提取量化参数的工作。要进行算法的硬件实现，要有对标的算法去验证硬件RTL的设计是否正确。这一部分要根据实际需求对模型进行训练和验证，为硬件上的实现提供参考依据。接下来需要对训练好的模型进行优化，将模型参数量缩小然后将浮点转为定点（也就是量化操作）。做好算法设计后再进行性能评估以确保模型达到预设的性能要求。
### 3.2硬件架构设计
&emsp;&emsp;架构工程师是一个FPGA神经网络加速器开发团队中的核心人物，负责设计整个加速器的整体架构，作各种权衡与取舍。架构工程师要有从上而下的设计思路，大到加速器需要执行哪些功能，是否需要设计外部的接口；小到算法与硬件中映射的关系，每一个具体处理步骤的加减乘除都需要进行考虑。架构工程师是软硬件沟通的桥梁，理论上要拥有整个开发流程中的所有技术储备，这样才能在项目初期以尽量少的算子实现神经网络的功能。
### 3.3硬件实现
&emsp;&emsp; 明确架构后才进入到具体的FPGA的硬件RTL设计。FPGA工程师的工作相对来说比较繁琐，要求能够使用硬件描述语言Verilog或SpianHDL设计卷积与需要的算子，也要能够调试PCIe、AXI或DDR接口电路等系统外围硬件接口。RTL设计的核心是FSM、RAM和寄存器的反复运用，实现硬件功能的方法不同最终也会导致系统效能上的差异。例如使用HLS进行开发的RTL电路一般会导致电路资源冗余和时序冗余，但使用Verilog进行硬件开发的难度与时间成本无疑更高。

### 3.4功能验证
&emsp;&emsp;在RTL代码设计完成之后，进行Testbench测试平台的搭建与验证是很有必要的，这包括了行为级的仿真和时序仿真。功能仿真是第一步，使用RTL仿真工具（如 ModelSim、Vivado Simulator）对Verilog或VHDL代码进行逻辑功能验证。设计者需要编写Testbench，模拟输入数据、权重、控制信号，并观测输出结果是否符合设计目标。在此阶段，可以对单个算子（如卷积、矩阵乘法）进行独立仿真验证，也可以对整个数据路径进行端到端验证，确保数据流、控制流完整且正确。时序仿真是在综合完成后，通过Post-Synthesis和Post-Implementation仿真验证设计是否满足时序要求。此环节关注数据延时、时钟稳定、跨时钟域通信等问题，确保设计在目标频率下运行稳定。使用Vivado Timing Analyzer可以分析关键路径、查找潜在的时序违例。

## 4.深度学习编译器
&emsp;&emsp;随着深度学习加速器和深度学习模型越来越复杂，所需的算力快速增长，对硬件部署提出了更高的要求。同一个模型往往需要在CPU、GPU、FPGA、TPU等多种硬件上高效运行。而每种硬件具有独特的计算架构、指令集、内存体系，导致传统模型移植和优化过程耗时耗力，难以适配日益复杂的算法。因此，深度学习编译器（Deep Learning Compiler）应运而生，旨在实现高效、跨平台、自动化的模型优化与部署。深度学习编译器是一种中间桥梁，介于深度学习框架（如TensorFlow、PyTorch）与目标硬件之间。它的作用是接收框架导出的模型，通过一系列编译流程，自动优化、调度，并生成面向特定硬件的高效代码。
### 4.1工作原理
&emsp;&emsp;深度学习编译器的工作是将前端算法先转化成中间层，进行优化和编译后输出合适的硬件代码。编译器首先从深度学习框架导入模型，如ONNX、TensorFlow SavedModel、PyTorch JIT等格式。它解析模型的计算图，将算子（如卷积、矩阵乘法、激活）和数据流转换为编译器内部的中间表示（IR）。然后在IR层，编译器将进行一系列图层级优化：如算子融（Operator Fusion将多个算子合并，减少内存访问与中间张量存储）和量化、剪枝（引入低比特量化、稀疏化支持）。最后编译器将优化后的IR转化为目标硬件代码，如LLVM IR、CUDA PTX、OpenCL、FPGA RTL、ARM汇编，最终生成可执行代码或库，并集成API供上层应用调用。

### 4.2适用于FPGA的深度学习编译器

#### 1.TVM：[TVM中文文档](https://tvm.hyper.ai/docs/)
TVM（Tensor Virtual Machine） 是一个开源、端到端的深度学习编译器框架，旨在帮助开发者高效地将深度学习模型部署到多种硬件平台（包括 CPU、GPU、FPGA、AI 加速器、微控制器等）。TVM 最初由亚马逊开源，现已成为 Apache 基金会孵化项目。TVM 独特的调度系统可以基于硬件特性（如缓存大小、SIMD 宽度、内存带宽）自动生成高效的内核代码，同时也允许开发者手动微调调度策略。TVM 提供自动调优工具（AutoTVM 和 Ansor），能够基于机器学习方法搜索最佳调度参数，从而进一步提升模型在目标硬件上的运行效率。TVM 也支持量化、稀疏计算、低比特运算等优化技术，使得模型在边缘设备、低功耗场景中依然能够保持高效推理性能。作为一个开放、模块化、可扩展的编译器框架，TVM 支持用户自定义算子、自定义硬件后端，适用于学术研究、工业部署以及AI芯片生态建设。凭借强大的自动化优化能力和跨平台支持，TVM 已成为目前深度学习模型部署领域的领先编译器之一，被广泛应用于云端推理、边缘AI、智能终端等场景。

#### 2.Vitis AI:[Vitis开发工具包](https://blog.csdn.net/falwat/article/details/104951145)
Vitis AI 是 Xilinx（现为 AMD）推出的面向 FPGA 和 SoC 平台的深度学习加速与编译工具链，旨在帮助开发者高效地将 AI 推理应用部署到 Xilinx 硬件（如 Zynq Ultrascale+ MPSoC、Versal ACAP、Alveo 数据中心加速卡）。Vitis AI 通过集成编译器、运行时和优化库，提供端到端的 AI 推理加速支持，简化了 FPGA 上 AI 模型部署的流程。

Vitis AI 支持主流深度学习框架（如 TensorFlow、PyTorch、Caffe），并能够将训练好的模型通过 Vitis AI Compiler 编译为 FPGA 端高效执行的指令和数据格式。编译器会自动进行图优化、算子融合、数据布局转换，并支持 INT8 量化，显著提升推理速度和降低功耗。结合 Vitis AI Runtime，开发者可以轻松通过 Python 或 C++ API 调用 FPGA 加速库，无需深入理解 FPGA 设计细节，从而实现软硬件解耦。

此外，Vitis AI 支持 DPU（Deep Learning Processing Unit）软核，该 IP 可以灵活部署在 FPGA 上，支持卷积、全连接、池化、激活等常见深度学习算子。Vitis AI 还配套提供模型评估、性能分析、工具链 GUI（如 Vitis AI Quantizer、Analyzer），方便开发者进行量化精度评估、性能调优和带宽优化。

#### 3.OpenVINO：[什么是OpenVino？](https://blog.csdn.net/bjbz_cxy/article/details/130318904)
OpenVINO（Open Visual Inference and Neural Network Optimization Toolkit） 是英特尔推出的开源深度学习推理优化工具套件，旨在加速 AI 模型在英特尔硬件上的高效部署。OpenVINO 支持多种英特尔硬件平台，包括 CPU、GPU、VPU（如 Myriad X）、FPGA，以及支持 OpenCL 的其他设备，帮助开发者将 AI 计算从云端拓展到边缘和终端设备。

OpenVINO 主要由 Model Optimizer、Inference Engine、Post-Training Optimization Tool、Pre-Trained Models Zoo 等组件组成。首先，Model Optimizer 将来自 TensorFlow、PyTorch、ONNX、Caffe 等框架的训练模型转换为 IR（Intermediate Representation）格式，并进行图优化，如常量折叠、算子融合、数据布局优化，以及量化支持。这样优化后的模型具备跨平台高效运行的能力。

接着，Inference Engine 负责将 IR 模型调度到目标硬件，并调用底层优化库（如 MKL-DNN、OpenCL、nGraph）执行高性能推理。Inference Engine 支持统一 API，开发者可基于 C++ 或 Python 快速部署模型，并支持多设备负载均衡、推理并行、动态批处理等高级特性。

OpenVINO 还提供 Post-Training Optimization Tool (POT)，支持模型后训练量化，进一步压缩模型并提升推理效率。此外，OpenVINO 结合 Intel DevCloud、OpenVINO Notebooks、Model Zoo 提供丰富的模型与开发资源，极大降低 AI 部署门槛。
